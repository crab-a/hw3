class Link:
    def compute(self, cluster, other):
        pass

class SingleLink:
    def compute(self, cluster, other):
        working_matrix = self.distance_matrix  # we need the distacne matrix here but its in data right now...